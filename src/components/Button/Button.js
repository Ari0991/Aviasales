import React from 'react';

import classes from './Button.module.scss';

const Button = () => {
  return <button className={classes['button--show-more']}>Показать еще 5 билетов!</button>;
};

export default Button;
